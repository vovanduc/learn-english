# Generate next lesson

Use this instruction in a planning chat when you want the next package generated:

> Generate the next lesson package in `GENERATION-QUEUE.md`. Use the corresponding British Council source material under `source/british-council/` as the source of truth. Create `README.md`, `SOURCE.md`, `PRE-CLASS.md`, `PROMPT.md`, `ANSWER-KEY.md`, `TRANSCRIPT.md`, and `SESSION-REPORT.md`. Do not invent missing source material. Add optional external listening only when it clearly supports the lesson topic and level. Keep the prompt compact enough for a separate Voice chat.
